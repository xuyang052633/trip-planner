{
  "name": "trip-planner-app",
  "version": "1.0.0",
  "description": "请假旅游规划助手",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "build": "echo 'No build step required'"
  },
  "dependencies": {
    "express": "^4.18.2",
    "amadeus": "^8.0.0",
    "cors": "^2.8.5"
  },
  "engines": {
    "node": ">=14.0.0"
  },
  "vercel": {
    "rewrites": [
      { "source": "/api/(.*)", "destination": "/server.js" },
      { "source": "/(.*)", "destination": "/public/index.html" }
    ]
  }
}
